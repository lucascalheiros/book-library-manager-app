android {
    signingConfigs {
        debug {
            storeFile file("booklib.debug.keystore.jks")
            storePassword "booklibkeypass"
            keyAlias "booklib.debug.key"
            keyPassword "booklibkeypass"
        }
    }
}